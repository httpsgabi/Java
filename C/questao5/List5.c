#define SUCESSO 0
#define _CRT_SECURE_NO_WARNINGS

#include <stdio.h>
#include <stdlib.h>

int calcularFatorial(n1){

    int i = n1;

    for(i > 0; i <= n1; i--){

        int nFatorial = n1 + (n1*i)
    }

    printf("O fatorial e:");

    return(calcularFatorial);
}

int main(int argc, char **argv)
{
    int n1;

    printf("Digite o numero para calcular seu fatorial: ");
    scanf("%i", n1);

    calcularFatorial(n1);

    return SUCESSO;
}
